pet sum(num) {
    out(num);
    term(num < 5){
        <- sum(num + 1);
    } else {
        <- 6;
    }
    
}
var p = sum(0);
out(p);
out("CIKLAS:")
loop (var a = 0; a < 10; a++) {
	var something = a * a;
    out(something);
}
out("a")